import { defineConfig } from 'vite'

export default defineConfig({
    // Set base to repository name for GitHub Pages
    // Replace 'repo-name' with your actual repository name
    // or use './' for relative paths if not using history mode routing
    base: './',
})
